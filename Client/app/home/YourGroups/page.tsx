import { FunctionComponent, ReactElement } from "react";
import YourGroups from "@/app/(Components)/Icons/YourGroups";

const Page: FunctionComponent = (): ReactElement => {
  return <YourGroups />;
};

export default Page;
