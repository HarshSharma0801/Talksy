import { FunctionComponent, ReactElement } from "react";
import MobileConversations from "@/app/(Components)/Sidebar/ConversationsMobile";

const Page: FunctionComponent = (): ReactElement => {
  return <MobileConversations />;
};

export default Page;
