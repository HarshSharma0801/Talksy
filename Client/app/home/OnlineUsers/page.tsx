import { FunctionComponent, ReactElement } from "react";
import OnlineUsers from "@/app/(Components)/Icons/OnlineUser";

const Page: FunctionComponent = (): ReactElement => {
  return <OnlineUsers />;
};

export default Page;
