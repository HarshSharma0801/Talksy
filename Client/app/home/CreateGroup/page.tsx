import { FunctionComponent, ReactElement } from "react";
import CreateGroup from "@/app/(Components)/Icons/CreateGroup";

const Page: FunctionComponent = (): ReactElement => {
  return <CreateGroup />;
};

export default Page;
